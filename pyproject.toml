[project]
name = "uv-template"
version = "0.1.0"
description = "Template repository for Python projects with uv"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
